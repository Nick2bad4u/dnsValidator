# Function: isPTRRecord()

```ts
function isPTRRecord(record: unknown): record is PTRRecord;
```

Defined in: [src/validators.ts:346](https://github.com/Nick2bad4u/dnsValidator/blob/main/src/validators.ts#L346)

Validates a PTR record

## Parameters

### record

`unknown`

## Returns

`record is PTRRecord`
