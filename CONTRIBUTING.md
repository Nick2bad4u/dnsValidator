# Contributing

Use Node.js 22 or newer and npm 11 or newer.

Before opening a pull request, run:

```sh
npm run release:verify
```

Keep changes focused, add or update tests for behavior changes, and do not commit generated cache or coverage output.
