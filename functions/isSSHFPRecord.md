# Function: isSSHFPRecord()

```ts
function isSSHFPRecord(record: unknown): record is SSHFPRecord;
```

Defined in: [src/dnssec-validators.ts:237](https://github.com/Nick2bad4u/dnsValidator/blob/main/src/dnssec-validators.ts#L237)

Validates an SSHFP record

## Parameters

### record

`unknown`

## Returns

`record is SSHFPRecord`
